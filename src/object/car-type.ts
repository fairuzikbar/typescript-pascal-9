// di Angular cuman ada model sama service
// untuk penulisan bisa gunakan user-credential.model
// kalo pake export enum CarType, waktu import harus ada { CarType }
// Kalo mau biasa ya pake export di bawah

export enum CarType {
    SUV = 'SUV',
    MVP = 'MVP',
    SEDAN = 'SEDAN',
    TRUCK = 'TRUCK'
}